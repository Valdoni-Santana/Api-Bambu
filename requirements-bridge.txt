# Dependências do microserviço bambu-bridge (FastAPI)
# Instale junto com a raiz do projeto para o pacote bambulab:
#   pip install -r requirements.txt -r requirements-bridge.txt

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
SQLAlchemy>=2.0.25
APScheduler>=3.10.4
requests>=2.25.0
Pillow>=10.0.0
